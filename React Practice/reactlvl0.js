console.log(React);
console.log(ReactDOM);


//Crreating root and connecting it to react.
const domElement = document.getElementById("dom-root");
const root=ReactDOM.createRoot(domElement);

console.log(root);